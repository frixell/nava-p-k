import styled from '@emotion/styled';

export const CvStructure = styled.section({
  display: 'flex',
  justifyContent: 'center',
  paddingTop: 0,
  width: '100%'
});

export const CvLeftColumn = styled.div({
  width: '99vw'
});

export const HeaderContainer = styled.header({
  marginTop: '3rem',
  width: '100%'
});

export const PageHeading = styled.h1({
  fontSize: '3rem',
  margin: 0,
  textAlign: 'center',
  color: 'var(--color-text-primary, #3a3a3a)'
});
